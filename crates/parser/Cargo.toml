[package]
name = "leo-parser"
version = "4.1.0"
authors = [ "The Leo Team <leo@provable.com>" ]
description = "Translating from the lossless syntax tree to the AST for the Leo language language"
homepage = "https://leo-lang.org"
repository = "https://github.com/ProvableHQ/leo"
keywords = [
  "aleo",
  "cryptography",
  "leo",
  "programming-language",
  "zero-knowledge"
]
categories = [ "compilers", "cryptography", "web-programming" ]
include = [ "Cargo.toml", "src", "README.md", "LICENSE.md" ]
license = "GPL-3.0"
edition = "2024"

[dependencies]
# leo dependencies
leo-ast             = { workspace = true }
leo-errors          = { workspace = true }
leo-parser-rowan    = { workspace = true }
leo-span            = { workspace = true }
# third party dependencies
indexmap            = { workspace = true }
itertools           = { workspace = true }

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
snarkvm             = { workspace = true }

[target.'cfg(target_arch = "wasm32")'.dependencies]
# Force `getrandom` 0.4's `wasm_js` backend on; pulled transitively by
# `snarkvm-console` (via leo-ast). The `snarkvm::*` facade itself comes
# through leo-ast's re-exports, so no direct slim-snarkvm deps here.
getrandom               = { version = "0.4", features = ["wasm_js"] }

[dev-dependencies]
# leo dependencies
leo-test-framework  = { workspace = true }
# third party dependencies
serial_test         = { workspace = true }
