program test.aleo{
	fn main(   a  :  u64  ,
	        b  :  u64   )  {

            assert(  a  >  0u64  )
                ;
    assert(
        b  >0u64)  ;

assert_eq(  a  ,
                b  )
                    ;

	    assert_eq(
	        a,
	            b)  ;

        assert_neq(
            a  ,
                0u64  )  ;
                assert_neq(a
    ,0u64)
        ;

	}
}
