/*
upgrade = { "mode": "custom" }
*/

program test.aleo {
    transition foo() {}
    
    async constructor() {
        assert_eq(true, true);
    }
    
    async constructor() {
        assert_eq(true, true);
    }
}