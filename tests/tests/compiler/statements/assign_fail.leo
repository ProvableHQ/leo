
program test.aleo {
    fn main(a: bool, x: u8, y: u8) {
        x::y = y;

        5u8 = y;

        x + x = y;

        -x = y;

        !x = y;

        a? x : x = y;

        x as u32 = y;

        [x, x, x] = y;

        (x, x, x) = y;

        x {x: y } = y;

        x() = y;

        4u8 = y;
    }
}
