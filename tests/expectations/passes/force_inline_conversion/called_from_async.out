program test.aleo {
    mapping counter: address => u64;
    async fn main(owner: address) -> Future<Fn()> {
        return async {
            let value: u64 = compute(5u64);
            __unresolved_set(counter, owner, value);
        };
    }
}
    fn compute(x: u64) -> u64 {
        return x * 2u64;
    }

