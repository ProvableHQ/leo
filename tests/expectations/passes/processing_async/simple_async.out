program test.aleo {
    mapping foo: u32 => u32;
    async fn main(x: u32, y: u32) -> Future<Fn()> {
        return test.aleo/main$0(x, y);
    }
}
    async fn main$0(x: u32, y: u32) {
        _mapping_set(test.aleo/foo, x, y);
    }

