program test.aleo {
    mapping foo: u32 => u32;
    fn main(x: u32, y: u32) -> Final<Fn()> {
        return async {
            let a = _mapping_get(test.aleo/foo, x);
            _mapping_set(test.aleo/foo, x, y);
        };
    }
}

