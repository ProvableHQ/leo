program test.aleo {
    fn increment() -> u64 {
        return 1u64;
    }
    fn add(a: u64, b: u64) -> u64 {
        return a + b;
    }
    fn main(x: u64) -> u64 {
        return test.aleo/increment();
    }
}

