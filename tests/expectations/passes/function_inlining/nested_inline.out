program test.aleo {
    inline helper(x: u32) -> u32 {
        return x + 1u32;
    }
    inline wrapper(y: u32) -> u32 {
        return (y + 1u32) * 2u32;
    }
    transition main(input: u32) -> u32 {
        return (input + 1u32) * 2u32;
    }
}
