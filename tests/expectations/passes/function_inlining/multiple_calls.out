program test.aleo {
    fn main(a: u32, b: u32) -> u32 {
        let $var$0 = a * a;
        let first: u32 = $var$0;
        let $var$1 = b * b;
        let second: u32 = $var$1;
        return first + second;
    }
}
    @inline
    fn square(x: u32) -> u32 {
        return x * x;
    }

