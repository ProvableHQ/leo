program test.aleo {
    struct u32? {
        is_some: bool,
        val: u32,
    }
    fn main(x: u32, flag: bool) -> u32 {
        let result: test.aleo::u32? = test.aleo::u32? { is_some: true, val: 5u32 };
        let $opt$0 = result;
        assert($opt$0.is_some);
        return $opt$0.val;
    }
}

