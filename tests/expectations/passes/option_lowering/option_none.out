program test.aleo {
    struct "u32?" {
        is_some: bool,
        val: u32,
    }
    fn main(flag: bool) -> u32 {
        let result: test.aleo/"u32?" = test.aleo/"u32?" { is_some: false, val: 0u32 };
        assert(result.is_some);
        return result.val;
    }
}

