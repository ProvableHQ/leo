program test.aleo {
    struct u32? {
        is_some: bool,
        val: u32,
    }
    fn main(x: u32, y: u32, d: u32) -> u32 {
        let a: test.aleo::u32? = test.aleo::u32? { is_some: true, val: x };
        let b: test.aleo::u32? = test.aleo::u32? { is_some: true, val: y };
        let $opt$0 = b;
        let $opt$1 = a;
        return $opt$1.is_some ? $opt$1.val : ($opt$0.is_some ? $opt$0.val : d);
    }
}

