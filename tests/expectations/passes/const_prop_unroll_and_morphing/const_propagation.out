program test.aleo {
    const a: u32 = 10u32;
    fn main(x: u32) -> u32 {
        let b: u32 = 15u32;
        return x + b;
    }
    fn foo(x: u32) -> u32 {
        let b: u32 = 15u32;
        {
            b = 42u32;
        }
        {
            b = 43u32;
        }
        return x + b;
    }
}

