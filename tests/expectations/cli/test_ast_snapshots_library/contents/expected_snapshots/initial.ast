library ast_library {
    export fn scale(x: u32, factor: u32) -> u32 {
        return x * factor;
    }
module helpers {
    export const UNIT: u32 = 1u32;
    export fn increment(x: u32) -> u32 {
        return x + UNIT;
    }
}

}
