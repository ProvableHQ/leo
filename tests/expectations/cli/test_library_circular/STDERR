--- add lib_b as dependency of lib_a ---
--- add lib_a as dependency of lib_b (creates cycle) ---
--- leo build lib_a (should fail: circular dependency) ---
Error [EPAK0375068]: circular dependency detected
     |
     = Break the cycle by removing one of the dependency edges in `program.json`. Programs cannot depend on themselves transitively.
