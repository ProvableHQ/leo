--- leo build (should fail: both main.leo and lib.leo exist) ---
Error [EPAK0375059]: source directory `TMPDIR/contents/ambiguous_pkg/src` contains both `main.leo` and `lib.leo`
     |
     = A package must be either a program or a library, not both. Remove one of the entry files.
