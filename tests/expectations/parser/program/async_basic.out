program test.aleo {
    mapping Yo: u32 => u32;
    async transition main() -> Future<Fn()> {
        let f: Future<Fn()> = finalize_main(1u32, 1u32);
        return f;
    }
    async function finalize_main(a: u32, b: u32) -> Future<Fn()> {
        _mapping_set(Yo, a, b);
    }
}

