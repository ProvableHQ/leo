program test.aleo {
    mapping Yo: address => u32;
    fn main(a: address) {
        assert_eq(a, _self_caller());
        assert_eq(a, _self_address());
        assert_eq(a, hello.aleo);
        return foo.aleo;
    }
}


