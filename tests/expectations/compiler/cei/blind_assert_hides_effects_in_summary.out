[WCEI03713002] Warning: the call to `caller.aleo::check_balance` runs after an interaction (`Final.run()`), and `caller.aleo::check_balance` itself performs checks or effects
    ╭─[ compiler-test:24:5 ]
    │
 24 │     check_balance(0u32);
    │ 
    │ Help: Move the call to `caller.aleo::check_balance` above the `.run()` call to avoid reentrancy issues.
    │ 
    │ Note: Under the Checks-Effects-Interactions pattern, any callee that performs checks or effects must run before any `.run()` calls in the same execution path.
────╯
program dep.aleo;

mapping dep_data:
    key as u32.public;
    value as u32.public;

function update:
    input r0 as u32.private;
    async update r0 into r1;
    output r1 as dep.aleo/update.future;

finalize update:
    input r0 as u32.public;
    set r0 into dep_data[0u32];

constructor:
    assert.eq edition 0u16;
// --- Next Program --- //
import dep.aleo;
program caller.aleo;

mapping balances:
    key as u32.public;
    value as u32.public;

function do_work:
    input r0 as u32.private;
    call dep.aleo/update r0 into r1;
    async do_work r1 r0 into r2;
    output r2 as caller.aleo/do_work.future;

finalize do_work:
    input r0 as dep.aleo/update.future;
    input r1 as u32.public;
    await r0;
    get.or_use balances[0u32] 0u32 into r2;
    gt r2 0u32 into r3;
    assert.eq r3 true;

constructor:
    assert.eq edition 0u16;
