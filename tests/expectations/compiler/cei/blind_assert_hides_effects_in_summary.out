[WCEI03713002] Warning: Call to `caller.aleo::check_balance` (which contains checks or effects) occurs after an interaction (Final.run()).
    ╭─[ compiler-test:21:5 ]
    │
 21 │     check_balance(0u32);
    │ 
    │ Help: Move this call before any calls to `.run()` to prevent reentrancy vulnerabilities.
────╯
program dep.aleo;

mapping dep_data:
    key as u32.public;
    value as u32.public;

function update:
    input r0 as u32.private;
    async update r0 into r1;
    output r1 as dep.aleo/update.future;

finalize update:
    input r0 as u32.public;
    set r0 into dep_data[0u32];
// --- Next Program --- //
import dep.aleo;
program caller.aleo;

mapping balances:
    key as u32.public;
    value as u32.public;

function do_work:
    input r0 as u32.private;
    call dep.aleo/update r0 into r1;
    async do_work r1 r0 into r2;
    output r2 as caller.aleo/do_work.future;

finalize do_work:
    input r0 as dep.aleo/update.future;
    input r1 as u32.public;
    await r0;
    get.or_use balances[0u32] 0u32 into r2;
    gt r2 0u32 into r3;
    assert.eq r3 true;
