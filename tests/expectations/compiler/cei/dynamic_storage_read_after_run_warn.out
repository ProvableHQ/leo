Warning [WCEI03713000]: Check operation `dynamic storage read `total`` occurs after an interaction (Final.run()).
    --> compiler-test:24:21
     |
  24 |     let val: u64? = Counter@(target)::total;
     |                     ^^^^^^^^^^^^^^^^^^^^^^^
     |
     = Move all checks (reads, asserts) before any calls to `.run()` to prevent reentrancy vulnerabilities.
program dep.aleo;

mapping dep_data:
    key as u32.public;
    value as u32.public;

function update:
    input r0 as u32.private;
    async update r0 into r1;
    output r1 as dep.aleo/update.future;

finalize update:
    input r0 as u32.public;
    set r0 into dep_data[0u32];

constructor:
    assert.eq edition 0u16;
// --- Next Program --- //
import dep.aleo;
program caller.aleo;

struct Optional__DmN5CQ9hzeK:
    is_some as boolean;
    val as u64;

mapping results:
    key as u32.public;
    value as u64.public;

function do_work:
    input r0 as u32.private;
    input r1 as field.private;
    call dep.aleo/update r0 into r2;
    async do_work r2 r1 into r3;
    output r3 as caller.aleo/do_work.future;

finalize do_work:
    input r0 as dep.aleo/update.future;
    input r1 as field.public;
    await r0;
    contains.dynamic r1 'aleo' 'total__'[false] into r2;
    get.or_use.dynamic r1 'aleo' 'total__'[false] 0u64 into r3 as u64;

constructor:
    assert.eq edition 0u16;
