program child.aleo;

struct DeepPoint__1R89g18Fnth:
    x as u32;
    y as u32;

function make_point_deep:
    input r0 as u32.private;
    input r1 as u32.private;
    cast r0 r1 into r2 as DeepPoint__1R89g18Fnth;
    output r2 as DeepPoint__1R89g18Fnth.private;

constructor:
    assert.eq edition 0u16;
// --- Next Program --- //
import child.aleo;
program parent.aleo;

function use_deep_type:
    input r0 as child.aleo/DeepPoint__1R89g18Fnth.private;
    add r0.x r0.y into r1;
    output r1 as u32.private;

function make_deep_struct:
    input r0 as u32.private;
    input r1 as u32.private;
    cast r0 r1 into r2 as child.aleo/DeepPoint__1R89g18Fnth;
    output r2 as child.aleo/DeepPoint__1R89g18Fnth.private;

function call_child_deep:
    input r0 as u32.private;
    input r1 as u32.private;
    call child.aleo/make_point_deep r0 r1 into r2;
    output r2 as child.aleo/DeepPoint__1R89g18Fnth.private;

constructor:
    assert.eq edition 0u16;


---
Note: Treating dependencies as Aleo produces different results:

[ETYC0372017] Error: the type `child.aleo::outer::inner::DeepPoint` is not found in the current scope
   ╭─[ compiler-test:7:22 ]
   │
 7 │     fn use_deep_type(p: child.aleo::outer::inner::DeepPoint) -> u32 {
   │ 
   │ Help: Check the type name for typos. If it comes from another program, qualify it with the program name, e.g. `credits.aleo::credits` instead of `credits`.
───╯
[ETYC0372017] Error: the type `child.aleo::outer::inner::DeepPoint` is not found in the current scope
   ╭─[ compiler-test:8:16 ]
   │
 8 │         return p.x + p.y;
   │ 
   │ Help: Check the type name for typos. If it comes from another program, qualify it with the program name, e.g. `credits.aleo::credits` instead of `credits`.
───╯
[ETYC0372017] Error: the type `child.aleo::outer::inner::DeepPoint` is not found in the current scope
   ╭─[ compiler-test:8:22 ]
   │
 8 │         return p.x + p.y;
   │ 
   │ Help: Check the type name for typos. If it comes from another program, qualify it with the program name, e.g. `credits.aleo::credits` instead of `credits`.
───╯
[ETYC0372017] Error: the type `child.aleo::outer::inner::DeepPoint` is not found in the current scope
    ╭─[ compiler-test:12:44 ]
    │
 12 │     fn make_deep_struct(x: u32, y: u32) -> child.aleo::outer::inner::DeepPoint {
    │ 
    │ Help: Check the type name for typos. If it comes from another program, qualify it with the program name, e.g. `credits.aleo::credits` instead of `credits`.
────╯
[ETYC0372005] Error: unknown struct or record `child.aleo::outer::inner::DeepPoint`
    ╭─[ compiler-test:13:16 ]
    │
 13 │         return child.aleo::outer::inner::DeepPoint { x: x, y: y };
    │ 
    │ Help: Check `child.aleo::outer::inner::DeepPoint` for typos and confirm it is declared in this scope. If it lives in another program, import it with the program-qualified name (e.g. `credits.aleo::credits`).
────╯
[ETYC0372017] Error: the type `child.aleo::outer::inner::DeepPoint` is not found in the current scope
    ╭─[ compiler-test:17:43 ]
    │
 17 │     fn call_child_deep(x: u32, y: u32) -> child.aleo::outer::inner::DeepPoint {
    │ 
    │ Help: Check the type name for typos. If it comes from another program, qualify it with the program name, e.g. `credits.aleo::credits` instead of `credits`.
────╯
[ETYC0372003] Error: expected type `child.aleo::outer::inner::DeepPoint`, found type `child.aleo::DeepPoint__1R89g18Fnth`
    ╭─[ compiler-test:18:16 ]
    │
 18 │         return child.aleo::make_point_deep(x, y);
    │ 
    │ Help: Change the expression to produce a `child.aleo::outer::inner::DeepPoint`, or cast it with `as child.aleo::outer::inner::DeepPoint` if a conversion exists.
────╯
