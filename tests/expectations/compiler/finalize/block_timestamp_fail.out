[ETYC0372194] Error: `std::ctx::block_timestamp()` must be inside a `final fn`, a `final` block, or a `view fn`
   ╭─[ compiler-test:3:30 ]
   │
 3 │         assert_eq(timestamp, std::ctx::block_timestamp());
   │ 
   │ Help: Move the `std::ctx::block_timestamp()` call into a `final fn`, a `view fn`, or wrap it in a `final { … }` block.
───╯
