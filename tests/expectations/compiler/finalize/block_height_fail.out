[ETYC0372034] Error: `std::ctx::block_height()` must be inside a `final fn`, a `final` block, or a `view fn`
   ╭─[ compiler-test:3:27 ]
   │
 3 │         assert_eq(height, std::ctx::block_height());
   │ 
   │ Help: Move the `std::ctx::block_height()` call into a `final fn`, a `view fn`, or wrap it in a `final { … }` block.
───╯
