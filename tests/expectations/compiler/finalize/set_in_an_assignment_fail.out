[EAST0372017] Error: the name `result` is defined multiple times
    ╭─[ compiler-test:14:9 ]
    │
 14 │     let result: u128 = Mapping::set(amounts, addr, amount);
    │
    ├─[ compiler-test:14:9 ]
    │
 13 │     let result: () = Mapping::set(amounts, addr, amount);
    │         ───┬──  
    │            ╰──── previous definition of `result` here
 14 │     let result: u128 = Mapping::set(amounts, addr, amount);
    │         ───┬──  
    │            ╰──── `result` redefined here
    │ 
    │ Help: Rename or remove all but one of the `result` definitions so the name is unique within its scope.
────╯
